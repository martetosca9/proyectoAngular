import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule } from '@angular/forms'; // Importa el módulo necesario para formularios

import { FormComponent } from './formulario.component.ts';

@NgModule({
  declarations: [
    
  ],
  imports: [CommonModule, FormsModule, ReactiveFormsModule],
})
export class FormsModule {}
